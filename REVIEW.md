# REVIEW.md — 구현 결과와 검토 포인트

PRD v1.0 기준 1차 구현 결과. 코덱스 리뷰 시 아래 "확인해 주셨으면 하는 곳"을 먼저 봐 주세요.

## 구현 범위

| Phase | 상태 | 메모 |
|---|---|---|
| 1. 게임 로직 | 완료 | `src/game` 순수 모듈 + 단위 테스트 44개 통과 |
| 2. PC 플레이 | 완료 | 3개 난이도, 타이머, 결과 모달, `R` 키 |
| 3. 터치 대응 | 완료 (실기기 확인 필요) | 길게 누르기 400ms, 10px 취소, 깃발 모드, 스크롤·확대 |
| 4. 학생 등록·기록 저장 | 완료 (에뮬레이터 테스트 필요) | 트랜잭션 1회 갱신, 재전송 대기열, 보안 규칙 |
| 5. 대시보드 | 완료 | 실시간 구독, 반 필터, 내 순위 고정, 이름 마스킹 |
| 6. 교사 관리·배포 | 완료 | Google 로그인, 삭제, CSV, 시즌, 설정, `vercel.json` |
| 7. P1 | 완료 | 원리 보기 모드, TV 모드(`?tv=1`), 효과음·진동 |

P2(no-guess 보드, DFS/BFS 비교, 사용자 지정 크기)는 구현하지 않았습니다.

## PRD와 다르게 구현한 부분

1. **보안 규칙의 `update` 조건 수정 (`firestore.rules`)**
   PRD 9.1 초안은 `wins + 1` 분기에서 항상 `bestTimeMs <= 기존 기록` 을 요구합니다.
   그러면 **"승리했지만 최고 기록은 못 깬 판"이 저장되지 않습니다.** (승수도 도전 수도 올릴 수 없음)
   "최고 기록 유지" 경우를 함께 허용하도록 고쳤습니다. 규칙 파일에 주석으로 이유를 남겼습니다.

2. **`students.classNo` 검증 완화**
   학번에서 반을 문자열 슬라이싱으로 다시 계산해 비교하는 방식은 규칙 문법 의존도가 높아,
   `1 <= classNo <= 20` 범위 검사로 바꿨습니다.

3. **승리 시 자동 깃발**
   승리하면 남은 지뢰에 깃발을 자동으로 표시합니다. (일반적인 지뢰찾기 관례, 남은 지뢰 수 0으로 표시)

4. **깃발은 첫 클릭 전에도 꽂을 수 있음**
   타이머는 첫 "열기"에서 시작한다는 규칙은 그대로입니다.

5. **오프라인 모드**
   `VITE_FIREBASE_*` 가 없으면 앱이 죽지 않고 게임만 가능한 모드로 동작합니다.
   Firebase 설정 전에도 수업에 쓸 수 있게 하기 위함입니다.

## 설계 메모

- **연쇄 열기**는 큐(BFS) 반복문입니다. 고급(16×16)에서도 스택 오버플로가 없고,
  열린 순서를 배열로 돌려주어 **원리 보기 모드 애니메이션에 그대로 재사용**합니다. (`ActionResult.opened`)
- **첫 클릭 보호**는 `placeMines(board, safeRow, safeCol, seed, mines)` 에서 안전 영역 9칸을 후보에서 빼고
  Fisher–Yates 셔플로 뽑습니다. 시드가 같으면 같은 보드가 재현됩니다. (테스트 1,000회 반복)
- **승리 판정**은 난이도 설정값이 아니라 **보드의 실제 지뢰 수**를 셉니다.
  손으로 만든 보드(테스트·수업 시연)에서도 규칙이 그대로 성립합니다.
- **입력 처리**(`useCellInput`)는 pointer 이벤트 하나로 마우스·터치를 함께 다룹니다.
  길게 눌러 깃발이 꽂히면 `handled` 플래그로 손 뗄 때의 열기를 막습니다.
- **번들 분리**: 첫 로딩을 줄이려고 순위·관리 화면을 lazy import 하고 react/firebase 청크를 나눴습니다.
  (초기 진입 시 앱 13KB + react 53KB gzip, firebase는 기록 저장이 필요한 시점의 청크)

## 확인해 주셨으면 하는 곳 (코덱스 리뷰 요청)

1. `src/game/board.ts` — `chord()` 에서 깃발을 잘못 꽂은 채 지뢰가 열릴 때의 상태 전이
2. `src/hooks/useCellInput.ts` — 길게 누르기 / 스크롤 취소 / 깃발 후 열기 방지의 경계 조건
3. `src/hooks/useGame.ts` — 원리 보기 모드 애니메이션 중 입력 차단, 타이머 정리
4. `src/firebase/records.ts` — 트랜잭션 갱신 로직이 `firestore.rules` 와 정확히 맞는지
5. `firestore.rules` — 위 1번 수정이 부정 방지를 약화시키지 않는지
6. `src/pages/Game.tsx` — 반응형 칸 크기 계산(가로 모드·고급 난이도 스마트폰)

## 남은 일

- [ ] 실기기 확인: iOS Safari / Android Chrome (컨텍스트 메뉴, 더블탭 확대, 깃발 후 열림)
- [ ] Firebase 에뮬레이터로 보안 규칙 테스트 4종 (정상 저장 / 느린 기록 덮어쓰기 거절 / 다른 이름 거절 / 최소 시간 미만 거절)
- [ ] 기기 3대 이상 동시 플레이로 대시보드 실시간 반영 확인
- [ ] Vercel 환경 변수 등록 후 배포 URL 전체 흐름 점검
