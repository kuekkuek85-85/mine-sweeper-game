# REVIEW.md — 구현 결과와 검토 포인트

PRD v1.0 기준 1차 구현 결과. 코덱스 리뷰 시 아래 "확인해 주셨으면 하는 곳"을 먼저 봐 주세요.

## 구현 범위

| Phase | 상태 | 메모 |
|---|---|---|
| 1. 게임 로직 | 완료 | `src/game` 순수 모듈 + 단위 테스트 (전체 58개 통과) |
| 2. PC 플레이 | 완료 | 3개 난이도, 타이머, 결과 모달, `R` 키 |
| 3. 터치 대응 | 완료 (실기기 확인 필요) | 길게 누르기 400ms, 10px 취소, 깃발 모드, 스크롤·확대 |
| 4. 학생 등록·기록 저장 | 완료 | 트랜잭션 1회 갱신, 재전송 대기열, 보안 규칙 |
| 5. 대시보드 | 완료 | 실시간 구독, 반 필터, 내 순위 고정, 이름 마스킹 |
| 6. 교사 관리·배포 | 완료 | 핀 번호 인증, 삭제, CSV, 시즌, 설정, `vercel.json` |
| 7. P1 | 완료 | 원리 보기 모드, TV 모드(`?tv=1`), 효과음·진동 |

P2(no-guess 보드, DFS/BFS 비교, 사용자 지정 크기)는 구현하지 않았습니다.

## PRD와 다르게 구현한 부분

1. **보안 규칙의 `update` 조건 수정 (`firestore.rules`)**
   PRD 9.1 초안은 `wins + 1` 분기에서 항상 `bestTimeMs <= 기존 기록` 을 요구합니다.
   그러면 **"승리했지만 최고 기록은 못 깬 판"이 저장되지 않습니다.** (승수도 도전 수도 올릴 수 없음)
   "최고 기록 유지" 경우를 함께 허용하도록 고쳤습니다. 규칙 파일에 주석으로 이유를 남겼습니다.

2. **`students.classNo` 검증 완화**
   학번에서 반을 문자열 슬라이싱으로 다시 계산해 비교하는 방식은 규칙 문법 의존도가 높아,
   `1 <= classNo <= 20` 범위 검사로 바꿨습니다.

3. **승리 시 자동 깃발**
   승리하면 남은 지뢰에 깃발을 자동으로 표시합니다. (일반적인 지뢰찾기 관례, 남은 지뢰 수 0으로 표시)

4. **깃발은 첫 클릭 전에도 꽂을 수 있음**
   타이머는 첫 "열기"에서 시작한다는 규칙은 그대로입니다.

5. **교사 인증을 Google 로그인 → 핀 번호로 변경** (선생님 요청)
   PRD 6.8은 Google 계정 로그인 + `admins` 컬렉션을 지정하지만, UID 등록·승인 도메인 설정이 번거로워
   핀 번호 방식으로 바꿨다. 다만 **핀을 화면 잠금으로만 쓰면 기록 삭제 보호가 사라지므로**,
   익명 로그인 + 보안 규칙 대조로 서버 검증을 유지했다.
   - 핀은 `config/secret` 에 두고 규칙에서 `allow read: if false` — 앱 코드에도, 네트워크 응답에도 나오지 않는다
   - 보안 규칙 안의 `get()` 은 읽기 권한과 무관하게 값을 볼 수 있어 대조가 가능하다
   - 한계: 규칙은 시도 횟수를 제한하지 못해 무차별 대입을 완전히 막지는 못한다. 핀을 길게 잡는 것을 권한다
   - `admins/{uid}` 경로도 그대로 인정해 비상 접근 수단을 남겼다

6. **오프라인 모드**
   `VITE_FIREBASE_*` 가 없으면 앱이 죽지 않고 게임만 가능한 모드로 동작합니다.
   Firebase 설정 전에도 수업에 쓸 수 있게 하기 위함입니다.

7. **시작 화면 등록 확인에 3초 타임아웃** (`src/firebase/students.ts`)
   Firestore 는 연결이 끊겨도 자체 타임아웃(약 10초)까지 응답을 붙잡고 있어,
   네트워크가 끊긴 상태에서 "시작하기"를 누르면 **15초 넘게 화면이 멈춰 있었습니다.**
   한 반 30명이 동시에 겪으면 PRD 1.2 "조작 문제로 수업이 멈추지 않는다" 기준을 어깁니다.
   3초 안에 응답이 없으면 오프라인으로 보고 게임에 들여보냅니다. (실측 15.2초 → 3.1초)
   이때 학번 도용 검사를 건너뛰지만, 보안 규칙이 서버에서 `name == 등록된 이름` 을
   다시 확인하므로 잘못된 기록은 저장되지 않습니다.

8. **물음표(?) 표시 추가** (선생님 요청)
   PRD 3.4는 조작 단순화를 위해 제외한 기능이지만, 일반적인 지뢰찾기 규칙에 맞춰 넣었습니다.
   대신 **설정에서 끌 수 있게** 해서 PRD의 두 단계 조작으로 언제든 돌아갈 수 있습니다. (기본값: 켬)
   - 닫힘 → 🚩 → ❓ → 닫힘 세 단계 토글
   - 물음표는 남은 지뢰 수에 세지 않고, 열기·연쇄 열기·코드 열기를 막지 않는다 (Windows 동작과 동일)
   - 깃발만 "열리지 않게 보호"하는 역할을 유지한다

9. **양클릭(좌+우 동시) 코드 열기 추가** (선생님 요청)
   PRD 5.1은 "열린 숫자 칸 왼쪽 클릭"만 정하지만, Windows 지뢰찾기에 익숙한 학생을 위해 양클릭도 받는다.
   - 양클릭만 `mousedown` 으로 처리한다. 포인터가 이미 눌린 상태에서 두 번째 버튼을 누르면
     **`pointerdown` 이 발생하지 않기 때문**이다. (Pointer Events 명세상 첫 버튼만 pointerdown 을 낸다)
   - 양클릭 뒤에는 두 버튼을 모두 뗄 때까지 열기·깃발을 막는다. 안 막으면 버튼을 뗄 때 열기가 한 번 더 일어난다.
   - 닫힌 칸에서는 아무 일도 하지 않는다. (`chordAt` 은 `open` 과 달리 닫힌 칸을 열지 않는다)

## 설계 메모

- **연쇄 열기**는 큐(BFS) 반복문입니다. 고급(16×16)에서도 스택 오버플로가 없고,
  열린 순서를 배열로 돌려주어 **원리 보기 모드 애니메이션에 그대로 재사용**합니다. (`ActionResult.opened`)
- **첫 클릭 보호**는 `placeMines(board, safeRow, safeCol, seed, mines)` 에서 안전 영역 9칸을 후보에서 빼고
  Fisher–Yates 셔플로 뽑습니다. 시드가 같으면 같은 보드가 재현됩니다. (테스트 1,000회 반복)
- **승리 판정**은 난이도 설정값이 아니라 **보드의 실제 지뢰 수**를 셉니다.
  손으로 만든 보드(테스트·수업 시연)에서도 규칙이 그대로 성립합니다.
- **입력 처리**(`useCellInput`)는 pointer 이벤트 하나로 마우스·터치를 함께 다룹니다.
  길게 눌러 깃발이 꽂히면 `handled` 플래그로 손 뗄 때의 열기를 막습니다.
- **네트워크 지연 대비**: `withTimeout` 으로 "기다리지 않고 오프라인으로 진행"을 한 곳에 모았습니다.
  원래 작업을 취소하지는 않아서(Firestore 쓰기는 뒤늦게라도 전송되는 편이 낫다) 늦은 실패만 흡수합니다.
- **양클릭 처리**: 헤드리스 브라우저로 실제 이벤트 순서를 찍어 보고 설계했습니다.
  좌클릭 후 우클릭 시 `pointerdown`(좌) → `mousedown`(좌) → `mousedown`(우, buttons=3) → `pointerup`(좌, buttons=0) 순서라,
  `mousedown` 에서 `buttons === 3` 을 보고 판단합니다.
- **번들 분리**: 첫 로딩을 줄이려고 순위·관리 화면을 lazy import 하고 react/firebase 청크를 나눴습니다.
  (초기 진입 시 앱 13KB + react 53KB gzip, firebase는 기록 저장이 필요한 시점의 청크)

## 확인해 주셨으면 하는 곳 (코덱스 리뷰 요청)

1. `src/game/board.ts` — `chord()` 에서 깃발을 잘못 꽂은 채 지뢰가 열릴 때의 상태 전이
2. `src/hooks/useCellInput.ts` — 길게 누르기 / 스크롤 취소 / 깃발 후 열기 방지의 경계 조건
3. `src/hooks/useGame.ts` — 원리 보기 모드 애니메이션 중 입력 차단, 타이머 정리
4. `src/firebase/records.ts` — 트랜잭션 갱신 로직이 `firestore.rules` 와 정확히 맞는지
5. `firestore.rules` — 위 1번 수정이 부정 방지를 약화시키지 않는지
6. `firestore.rules` / `src/firebase/auth.ts` — 핀 기반 교사 세션에 우회 경로가 없는지
7. `src/firebase/students.ts` — 3초 타임아웃이 학번 도용 차단(PRD 6.2)을 얼마나 느슨하게 만드는지
8. `src/pages/Game.tsx` — 반응형 칸 크기 계산(가로 모드·고급 난이도 스마트폰)

## 남은 일

- [ ] 실기기 확인: iOS Safari / Android Chrome (컨텍스트 메뉴, 더블탭 확대, 깃발 후 열림)
- [x] Firebase 에뮬레이터로 보안 규칙 테스트 — `npm run test:rules` 33개 통과
      (정상 저장 / 느린 기록 덮어쓰기 거절 / 다른 이름 거절 / 최소 시간 미만 거절 + 교사 핀 인증 포함)
- [ ] 기기 3대 이상 동시 플레이로 대시보드 실시간 반영 확인
- [ ] Vercel 환경 변수 등록 후 배포 URL 전체 흐름 점검
